<!-- Modal -->
<div class="modal fade" id="modal4" role="dialog">
    <div class="modal-dialog modal-lg">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">เหตุแห่งการเลื่อนคดี/ยกเลิกวันนัด (คดี)</h4>
            </div>
            <form>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label for="validationServer04">ประเภทการเลื่อนคดี</label>
                            <div class="form-group">
                                <select multiple size="14" class="form-control" id="exampleFormControlSelect1">
                                    <% changeAppointType.forEach(function(list) { %>
                                        <option>
                                            <%= list.name %>
                                        </option>
                                        <% }); %>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-8 mb-3">
                            <label for="validationServer09">หมายเหตุ</label>
                            <textarea class="form-control" id="exampleFormControlTextarea1" rows="14"></textarea>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-primary set-left" type="submit">Submit form</button>
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>
            </form>
        </div>

    </div>
</div>

<!-- End Modal -->